const path = require('path');

function showIndexDotHtml(req,res) {
    res.sendFile(path.join(__dirname , ".." , "views", "index.html"));
}

function insertRec(req,res) {
}

function showRec(req,res) {
}

function deleteRec(req,res) {
  }
  
module.exports = {
    showIndexDotHtml,
    insertRec,
    showRec,
    deleteRec
};
